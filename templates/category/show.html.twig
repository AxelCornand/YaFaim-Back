{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
<div class="container">
    <h1>Catégorie</h1>
<a href="{{ path('app_category_index') }}" class="btn btn-sm btn-success">Revenir à la liste</a>
<a href="{{ path('app_category_edit', {'id': category.id}) }}" class="btn btn-sm btn-warning">Modifier</a>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
